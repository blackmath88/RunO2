# Current setup status

The repository has been reorganized for the runO2 project, but the Claude-generated implementation is still stored as `basel-spatial-graph-clean-air-run.zip` at the repository root because this connector cannot unpack binary ZIP contents.

Once unpacked, validate the generated implementation before changing architecture.
